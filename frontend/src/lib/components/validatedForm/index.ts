import Root from './validatedForm.svelte';

/**
 * The validated form component makes sure all its child validated inputs are valid before allowing
 * submission.
 * See storybook for details.
 */
export {
	Root,
	//
	Root as ValidatedForm
};
