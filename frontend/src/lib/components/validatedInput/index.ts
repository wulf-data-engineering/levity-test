import Root from './validatedInput.svelte';

/**
 * The validated input component wraps a shadcn/svelte input with label, validation and
 * informational texts.
 * See storybook for details.
 */
export {
	Root,
	//
	Root as ValidatedInput
};
